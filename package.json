{
  "name": "echo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @echo/web",
    "dev:api": "npm run dev --workspace @echo/api",
    "build": "npm run build --workspace @echo/web"
  }
}
